# Next.js Build Environment Configuration
# These are only needed during development/build time
# In production, the frontend is served by FastAPI

# Feature Flags (development only)
NEXT_PUBLIC_DEBUG_MODE=true

# Build Configuration
ANALYZE=false
BUNDLE_ANALYZE=false